---
import Layout from '../layouts/Layout.astro';
---

<Layout title="Early Access - OmenDB" description="Get early access to the fastest database built with machine learning">
  <section class="py-20 bg-gradient-to-br from-blue-50 to-indigo-100 min-h-screen flex items-center">
    <div class="max-w-4xl mx-auto px-4 sm:px-6 lg:px-8 text-center">
      <h1 class="text-4xl md:text-6xl font-bold text-gray-900 mb-6">
        Get Early Access
      </h1>

      <p class="text-xl md:text-2xl text-gray-600 mb-8 max-w-2xl mx-auto">
        Be the first to try our database-as-a-service when it launches.
        Built from the ground up for <strong>10x faster</strong> performance.
      </p>

      <div class="bg-white rounded-lg shadow-lg p-8 max-w-md mx-auto mb-12">
        <form id="earlyAccessForm">
          <div class="mb-6">
            <label for="email" class="block text-sm font-medium text-gray-700 mb-2">Email Address</label>
            <input
              type="email"
              id="email"
              name="email"
              required
              class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:ring-blue-500 focus:border-blue-500"
              placeholder="your@email.com"
            />
          </div>

          <div class="mb-6">
            <label for="usecase" class="block text-sm font-medium text-gray-700 mb-2">Primary Use Case</label>
            <select
              id="usecase"
              name="usecase"
              class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:ring-blue-500 focus:border-blue-500"
            >
              <option value="">Select your use case</option>
              <option value="time-series">Time-series data (IoT, metrics)</option>
              <option value="financial">Financial data (trading, analytics)</option>
              <option value="ecommerce">E-commerce (product catalogs)</option>
              <option value="analytics">Real-time analytics</option>
              <option value="other">Other</option>
            </select>
          </div>

          <div class="mb-6">
            <label for="company" class="block text-sm font-medium text-gray-700 mb-2">Company/Organization (Optional)</label>
            <input
              type="text"
              id="company"
              name="company"
              class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:ring-blue-500 focus:border-blue-500"
              placeholder="Your company"
            />
          </div>

          <button
            type="submit"
            class="w-full bg-blue-600 text-white py-3 px-6 rounded-lg font-semibold hover:bg-blue-700 transition-colors"
          >
            Join Waitlist
          </button>
        </form>

        <div id="successMessage" class="hidden mt-6 p-4 bg-green-100 border border-green-400 rounded-lg">
          <p class="text-green-700 font-medium">Thanks! We'll be in touch when we launch.</p>
        </div>
      </div>

      <div class="grid grid-cols-1 md:grid-cols-3 gap-8 mb-12">
        <div class="text-center">
          <div class="w-16 h-16 bg-blue-100 text-blue-600 rounded-full flex items-center justify-center mx-auto mb-4 text-2xl">⚡</div>
          <h3 class="text-lg font-bold text-gray-900 mb-2">10x Faster</h3>
          <p class="text-gray-600">Learned indexes eliminate tree traversal</p>
        </div>
        <div class="text-center">
          <div class="w-16 h-16 bg-green-100 text-green-600 rounded-full flex items-center justify-center mx-auto mb-4 text-2xl">🔄</div>
          <h3 class="text-lg font-bold text-gray-900 mb-2">Drop-in Replacement</h3>
          <p class="text-gray-600">PostgreSQL wire protocol compatible</p>
        </div>
        <div class="text-center">
          <div class="w-16 h-16 bg-purple-100 text-purple-600 rounded-full flex items-center justify-center mx-auto mb-4 text-2xl">🤖</div>
          <h3 class="text-lg font-bold text-gray-900 mb-2">Automatic ML</h3>
          <p class="text-gray-600">No model tuning or configuration needed</p>
        </div>
      </div>

      <div class="bg-white rounded-lg p-8 mb-8">
        <h2 class="text-2xl font-bold text-gray-900 mb-4">What You'll Get</h2>
        <ul class="text-left text-gray-700 space-y-3 max-w-2xl mx-auto">
          <li class="flex items-start">
            <span class="text-green-500 mr-3 mt-1">✓</span>
            <span><strong>Early beta access</strong> to our managed database service</span>
          </li>
          <li class="flex items-start">
            <span class="text-green-500 mr-3 mt-1">✓</span>
            <span><strong>Free tier</strong> during beta testing period</span>
          </li>
          <li class="flex items-start">
            <span class="text-green-500 mr-3 mt-1">✓</span>
            <span><strong>Direct access</strong> to our engineering team for feedback</span>
          </li>
          <li class="flex items-start">
            <span class="text-green-500 mr-3 mt-1">✓</span>
            <span><strong>Migration assistance</strong> from your existing database</span>
          </li>
          <li class="flex items-start">
            <span class="text-green-500 mr-3 mt-1">✓</span>
            <span><strong>Founder pricing</strong> when we launch publicly</span>
          </li>
        </ul>
      </div>

      <div class="text-center">
        <p class="text-gray-600 mb-4">Want to try our PostgreSQL extension now?</p>
        <div class="flex flex-col sm:flex-row gap-4 justify-center">
          <a href="/demo" class="bg-gray-900 text-white px-6 py-3 rounded-lg font-semibold hover:bg-gray-800 transition-colors">
            Try the Demo
          </a>
          <a href="https://github.com/omendb/omendb" class="bg-white text-gray-900 px-6 py-3 rounded-lg font-semibold border border-gray-300 hover:bg-gray-50 transition-colors">
            View on GitHub
          </a>
        </div>
      </div>
    </div>
  </section>
</Layout>

<script>
  const form = document.getElementById('earlyAccessForm') as HTMLFormElement;
  const successMessage = document.getElementById('successMessage') as HTMLElement;

  form.addEventListener('submit', async (e) => {
    e.preventDefault();

    const formData = new FormData(form);
    const data = {
      email: formData.get('email'),
      usecase: formData.get('usecase'),
      company: formData.get('company'),
      timestamp: new Date().toISOString(),
      source: 'omendb.io'
    };

    // In a real implementation, you'd send this to your backend
    // For now, we'll just simulate success and store locally
    console.log('Early access signup:', data);

    // Store locally for demo purposes
    const existingSignups = JSON.parse(localStorage.getItem('omendb_signups') || '[]');
    existingSignups.push(data);
    localStorage.setItem('omendb_signups', JSON.stringify(existingSignups));

    // Show success message
    form.style.display = 'none';
    successMessage.classList.remove('hidden');

    // Track with analytics (would be Google Analytics, Mixpanel, etc.)
    if (typeof gtag !== 'undefined') {
      gtag('event', 'early_access_signup', {
        event_category: 'engagement',
        event_label: data.usecase || 'unknown'
      });
    }
  });
</script>