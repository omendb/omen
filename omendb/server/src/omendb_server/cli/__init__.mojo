"""
CLI module for OmenDB embedded mode operations.

This module provides command-line interface tools for working with OmenDB
embedded databases. It supports dual-mode design principles, working with
both embedded file storage and server backends through the same interface.
"""